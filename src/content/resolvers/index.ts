/**
 * Content Resolvers Index
 * Re-exports all resolver functions
 */

export {
  initContentResolver,
  hasDatabaseContent,
  resolveHeroContent,
  resolveDestinations,
  resolveDestination,
  resolveHotels,
  resolveHotel,
  resolvePackages,
  resolvePackage,
  resolveExperiences,
  resolveTestimonials,
  resolveAddons,
  resolvePartners,
  getContentStats,
} from './contentResolver';
